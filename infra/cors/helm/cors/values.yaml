# Default values for the CORS middleware

fullnameOverride: "cors-header"
cors:
  # List of allowed origins for CORS
  allowedOrigins:
  - "https://localhost:5173" # Vite default port, but with https (using vite-plugin-mkcert)
